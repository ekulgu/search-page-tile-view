import React from "react";

const Button = () => {
  return (
    <button
      style={{
        background: "#6d51ff",
        color: "#fff",
        borderRadius: "3px",
        border: "none",
        fontSize: "13px",
        padding: "5px 15px"
      }}
    >
      Add Corpus
    </button>
  );
};

export default Button;
